kax-files

Requirements:
If not present, the Kieker Jar in the "lib" folder can be automatically built and copied using the build.gradle file of the kieker-examples. 

Description:
This example shows how to use kax-Files as source for Kieker filter settings.


1. Execute the gradle build:
     - Linux: ./gradlew runAnalysis
     - Windows: gradlew.bat runAnalysis

