Build with a call of ant.

Files to copy in the server lib directory:
  KickerInstr.jar       (the build of this example)
  kicker-*_aspectj.jar  (a current build of Kicker)

Add kicker-*_aspectj.jar as a javaagent.
