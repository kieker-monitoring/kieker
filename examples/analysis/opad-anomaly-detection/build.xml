<project name="Tutorial-Example" basedir="." default="run">

	<property file="build.properties" />

	<path id="classpath.local">
		<fileset dir="${lib.dir}" includes="**/*.jar" />
	</path>

	<condition property="not-all-files-available">
		<resourcecount property="num.requiredLibsFound" when="ne" count="2">
			<fileset dir="${lib.dir}">
				<include name="${jar.file.kieker}" />
				<include name="${jar.file.opencsv}" />
			</fileset>
		</resourcecount>
	</condition>

	<target name="-check-files" if="not-all-files-available">
		<fail message="${msg.filesNotFound}" />
	</target>

	<target name="run" depends="-check-files, -build-jar">
		<java fork="true" classname="${main-class}">
			<classpath>
				<path refid="classpath" />
				<path location="${jar.file}" />
			</classpath>
		</java>
	</target>

	<target name="-build-jar" depends="-compile">
		<jar destfile="${jar.file}" basedir="${build.dir}">
			<manifest>
				<attribute name="Main-Class" value="${main-class}" />
			</manifest>
		</jar>
		<delete dir="${build.dir}" />
	</target>

	<target name="-compile" depends="-init">
		<javac source="1.6" target="1.6" srcdir="${src.dir}" destdir="${build.dir}" includeAntRuntime="false">
			<classpath refid="classpath"/>
			<classpath refid="classpath.local"/>
		</javac>
	</target>

	<target name="-init">
		<delete dir="${build.dir}" />
		<mkdir dir="${build.dir}" />
	</target>

	<target name="clean">
		<delete file="${jar.file}" />
	</target>

</project>
