<project name="Tutorial-Example" basedir="." default="run">

  <property name="lib.dir" value="lib"/>
  <property name="build.dir" value="build"/>
  <property name="src.dir" value="src"/>
  <property name="jar.file" value="example.jar"/>
  <property name="main-class" value="mySimpleKiekerExample.bookstoreTracing.Bookstore"/>
  <property name="meta.dir" value="META-INF"/>
  <property name="log4jproperties.file" value="log4j.properties"/>
  
  <path id="classpath">
    <fileset dir="${lib.dir}" includes="**/*.jar"/>
  </path>
  
  <target name="run" depends="build-jar">
    <java fork="true" classname="${main-class}">
      <classpath>
	<path refid="classpath"/>
	<path location="${jar.file}"/>
      </classpath>
      <jvmarg value="-Dlog4j.configuration=${meta.dir}/${log4jproperties.file}"/>
    </java>
  </target>
  
  <target name="build-jar" depends="compile">
    <jar destfile="${jar.file}" basedir="${build.dir}">
      <manifest>
	<attribute name="Main-Class" value="${main-class}"/>
      </manifest>
    </jar>
    <delete dir="${build.dir}"/>
  </target>
  
  <target name="compile" depends="init">
      <javac srcdir="${src.dir}" destdir="${build.dir}" classpathref="classpath"/>
      <mkdir dir="${build.dir}/${meta.dir}"/>
  </target>
  
  <target name="init">
    <delete dir="${build.dir}"/>
    <mkdir dir="${build.dir}"/>
  </target>
  
</project>